{
  "source_type": "mcp",
  "status": "stubbed",
  "provider": "google_calendar",
  "transport": "streamable_http",
  "codex_server_name": "google-calendar",
  "remote_url": "https://gcal.mintmcp.com/mcp",
  "auth": "oauth",
  "current_mode": "mock_snapshot",
  "fallback_snapshot_path": "data/calendar.json",
  "notes": [
    "The app shape assumes calendar data will come from an MCP server once configured in Codex.",
    "Until credentials are wired, /api/calendar serves the local mock snapshot.",
    "If you add the MCP server in Codex, agents can later read live calendar data instead of the mock snapshot."
  ]
}
