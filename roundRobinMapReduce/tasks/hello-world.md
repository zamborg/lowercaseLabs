# Hello World Reduction Task

## Map Task

Create a valid Python `hello.py` program that prints `hello world`.
Write `RESULT.md` describing your candidate.

## Reduce Task

Inspect all mapper workspaces and choose the valid `hello.py` implementation with the fewest lines.
Copy the winner to the run root as `hello.py` and explain the selection in `RESULT.md`.
